<%= render './shared/banner_posts' %>
<div class="show-container">
  <div class="row">
      <% @posts.each do |p| %>
        <%= render './shared/card', p: p %>
      <% end %>
  </div>
  <%= link_to "Create a new post", new_post_path%>
</div>
